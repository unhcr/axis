
%header.container.full-width
  .account-menu
    .menu-item.ui-primary
      Ça va,
      %span
        = current_user.firstname
    .menu-item
      = link_to "Sign out", destroy_user_session_path, :method => :delete
  .row
    .col-md-4.col-md-offset-3
      .ui-medium-header.logo
        <span class="bold">VISIO</span><span class="light">UNHCR</span>
  .row.menu
    .search.menu-item.ui-primary.pull-right
      Search by Operation or Indicator
    %a.thematic-item.menu-item.ui-primary-light.pull-right
      .underline
        SGBV
      .theme-content
        SGBV
    %a.thematic-item.menu-item.ui-primary-light.pull-right
      .underline
        Education
      .theme-content
        Education
    %a.thematic-item.menu-item.ui-primary-light.pull-right
      .underline
        Child&nbsp;Protection
      .theme-content
        Child&nbsp;Protection



#map

:javascript
  $(document).ready(function() {

    window.router = new Visio.Routers.IndexRouter({
      mapMD5: '#{@mapMD5}'
    });
    Backbone.history.start();

  });
